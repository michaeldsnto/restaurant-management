import { Moon } from "lucide-react";

export default function NavbarThemeToggle() {

    return (

        <button className="
      rounded-2xl
      border
      border-zinc-800
      bg-zinc-900
      p-3
      transition-all
      hover:bg-zinc-800
    ">

            <Moon size={18} />

        </button>

    );
}